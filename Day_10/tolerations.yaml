apiVersion: v1 
kind: Pod
metadata:
  name: demo-tolert
  labels:
    app: production
spec:
  containers:
    - name: nginx
      image: nginx
  tolerations:
    - key: env
      operator: "Equal"
      value: production
      effect: "NoSchedule"
